$(document).ready(function () {
  $("#micropost_picture").bind("change", function() {
    var size_in_megabytes = this.files[0].size/ <%= Settings.byte_to_megabyte %>;
    if (size_in_megabytes > <%= Settings.limit_size_image %>) {
      alert("<%= I18n.t("reject_image_msg") %>");
    }
  });
});
